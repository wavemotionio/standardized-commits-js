"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _object = _interopRequireDefault(require("object.entries"));

var _availableRules = _interopRequireDefault(require("./availableRules"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const ruleWarningMessages = (input, config) => {
  let warningMessage = '';
  const configRuleEntries = (0, _object.default)(config.rules);
  configRuleEntries.forEach(rule => {
    const ruleName = rule[0];

    if (!(_availableRules.default[ruleName] instanceof Function)) {
      throw new Error(`The rule '${ruleName}' does not exist, look at the documentation to check valid rules.`);
    }

    const ruleIs = _availableRules.default[ruleName](input, config).check();

    if (!ruleIs) {
      warningMessage += `${_availableRules.default[ruleName](input, config).message()}\n`;
    }
  });
  return warningMessage;
};

var _default = ruleWarningMessages;
exports.default = _default;